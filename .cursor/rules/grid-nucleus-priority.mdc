---
description: 凝结核分列/分行原则是第一位；一切后处理/启发式不得覆盖
globs: codes/reconstruct/grid_nucleus/**/*
alwaysApply: true
---

# 凝结核 — 分割原则第一位（不可破坏）

## 总纲

**凝结核分割原则是第一位。永远优先。**

任何冲突时，以凝结核为准：质检标签、导出过滤、跨格标注、表头回挂、启发式剪槽、双边打分补丁，都不得覆盖或反向改写凝结核已定的行列。

`codes/reconstruct/grid_nucleus/` 的核心职责：按字框凝结核恢复行列。

### 凝结核是什么（定义，不可忘）

**凝结核 = 连续的文本或连续的数字，在几何上是一体。一体核优先于一切语义启发式。**

- 同行连续字/数 → 一个核，不得因启发式从中间拆开。
- 核的左右范围由字框 `[x0,x1]` 决定；后处理不得把一体核拆成两列内容。
- **禁止**用词表预判「这是分组标题」去拆核（如见到「资产」就拆成通栏行）——那会拆掉「资产管理计划」等一体核。

### 分列与分行是同一套道理

凝结核不只在**分列**时起作用，**分行**时也一样：看字框落在哪个**数据区域**，不靠文案猜。

| 源表形态 | 含义 | 做法 |
|----------|------|------|
| **多行文本落在同一数据区域**（同一逻辑格内折行，如「资产」+「管理计划」挤在科目格里） | 仍是一个（或应并回一个）凝结核/单元格 | 同簇同格拼回；扩该区域的行带/列带；**不要**拆成两行各当一项 |
| **独占一行**（该行自己就是一个数据区域，如单独一行「资产」，下一行才是「现金」+金额） | 各行各核 | **保留原分行**；禁止为「看起来像折行」拼成「资产现金」 |

**口诀：同一数据区域内的多行字 → 一体；独占一行的字 → 保持独占。**

判定依据是几何上的行带/列带与邻格有无独立核，不是词表（不能预判「资产一定是标题」或「资产一定要和下行拼」）。

### 分组标题：不可提前确定，依分割结果调整

**分组/小节标题不能靠词表事先认定。** 必须先按凝结核完成分行分列，再根据**分割结果**判定：

| 阶段 | 做什么 | 禁止 |
|------|--------|------|
| 分列 / 分行 / 聚类 | 只认几何一体核、数据区域与空隙 | 用「资产/负债/权益」等词表预判小节并拆行、拆核或强行拼行 |
| 网格已定之后 | 看该行是否仅一格有字、金额列是否空、核宽是否真正盖住多列有数据的列带 | 无核宽证据仍标 `⟦↔N⟧`；为凑「标题」把已并好的一体核再拆开；把独占行拼进下一行 |

真分组行的典型**结果特征**（事后）：旁列金额全空 + 凝结核宽度交叉多列有数据的列 → 再标跨列。不是先认定文案再改网格。

### 上下交叉时的判定（同列 vs 跨列）

上下两行的凝结核若 **水平有交叉**（`[x0,x1]` 重叠），只可能是两类，必须结合 **左右邻列的区域** 界定：

| 判定 | 含义 | 做法 |
|------|------|------|
| **同列** | 两核同属一列，只是宽窄/对齐不同（左缘科目 vs 右对齐金额、折行续文等） | **扩大该列凝结核的左右公共范围**（列带包络），两核落同一列；禁止中间插空白缝列 |
| **跨列** | 其中一个核真正盖住了左右多列有数据的区域 | **标该核为跨列**（`⟦↔N⟧`），不要把旁列有核的列并进主列 |

**判定口诀：先看交叉，再看左右列里有没有独立凝结核。**

- 交叉 + 旁列对应带内 **另有核** → 倾向跨列（或主列加宽到空带为止，但不吞旁列核）。
- 交叉 + 旁列对应带内 **无核/仅空白** → 倾向同列，扩大列带，收回空白缝。
- 禁止：只凭中心距/字数/语义猜跨列；禁止为「好看」拆开已交叉的同列核。

## 主：凝结核分割行列

### 顺序铁律

**文本的阅读顺序永远都不能改变。**  
只能上下左右整体平移，允许伸缩中间空白；绝对不能交换顺序。

- **允许**：整表/整列带平移；列间距、行间距胀缩。
- **禁止**：阅读序上左右/上下对调、粘连后重排、同格拼接时颠倒左右字。
- **同行阅读序**：同一视觉行内严格 **左→右（按 x0）**；不得因 y0 轻微抖动把右边字排到左边前面。  
  反例：PDF 为「注释 | 人民币」，拼成「人民币 注释」= 违规。
- **表界硬切（根除表外混入）**：按阅读序找到表起点（`序号/指标/单位/年头`），**起点之前的字一律不进表**；禁止事后把表外叙述并进表内单元格（不得增删改表内数据与顺序）。
- 反例：`无期限 | <6个月 | 6-12个月 | ≥1年` → `≥1年 无期限 <6个月` = 违规。
- 反例：把「…评估指标如下：」并进第 1 行指标格 = 违规。

### 分列铁律（由高到低）

0. **一体核优先**：连续文本/数字是一个凝结核；上下有交叉时，先按「同列扩范围 vs 跨列标注」判定（见总纲），再谈造槽/剪槽。**分组标题后置**：不得用词表预判小节去拆一体核；小节/通栏只在分列结果上按核宽与空列调整。
1. **同左右边界 → 必须同列**：`[x0,x1]` 实质相同（如折行「2023年」与「指标值」）落列后不得分列；不因中心打分、幽灵金额槽拆开。**指标列头与金额核交叉/同列廊**（`占有关同类`/`比例(%)` 与 `6.38`：左缘表头、右对齐数值）必须同列，禁止中间插空白列；指标头不参与造槽，列由金额凝结核定后再吸附。**金额列上的折行表头**（如「财务并表口径下」+「的资产负债表」）续文跟左缘接近的上行同列，表头按右缘贴齐下方金额列；吸附后去掉全程无核的幽灵列。
2. **同列取最小公共边界**：上下行同列核用公共左右包络；**缩进不是新列**（层次用 `indent_level` / 前导空格，落在已定科目列）。
3. **拆列只认同行大空隙**：同一行两段不连续文本之间有明显空白才拆列；**文本列重左缘**，连续中文同核/同格，不因右缘或空格拆列。**小空隙**须用上下单元格的整体列带约束，不得机械拆成多列。**拆出一列却全空**（通列无实质内容，仅空白/`⟦↔⟧`）→ **拆错了，必须收回**，不得保留空白缝列。
4. **列数以表体为准**：金额/占位/纯文本/文本+数字等有内容即成列；禁止跨列表头造幽灵空列；右对齐 `-` 归入已有列、不单独造槽。折行指标头（`占有关同类`/`交易发生额`/`比例(%)`）无金额表体时不得撑出空白缝列，应与右侧比例数值同列。
5. **表体优先于表头**：跨列表头只作标注参考，不得反向并掉/打乱表体列。
6. **序号列与科目列分离**；同格多核按阅读序：分行后同行内严格左→右（`x0`），禁止 `(y0,x0)` 因 y 抖动左右颠倒。
7. **折行 vs 层次**：旁列空的续行并入最近金额锚（上下扩）；以「的/及/与」等收尾的明确折行碎片，即使夹在两金额锚之间也并入下方金额行；其余两锚争抢 → `ambiguous` / 人工。**源表已是两行的完整短词**（如「资产」与「现金」）必须保留两行，禁止拼成「资产现金」。
8. **跨格标注服从分列（慎跨、有据必跨）**：默认不跨列；**仅当凝结核层面证明交叉且旁列区域内另有独立核**（字框确实吃进上下表体多列有数据的列）才标 `⟦↔N⟧`——有据就必须标（如「核心一级资本：」盖住序号+科目列）。禁止空邻格/字数/语义猜测强制跨列。主列取与核重叠最大的列；右缘毛刺不算。**短年头「2025年」**：字框虽窄，按其下金额列中心距最近年头归属，跨发生额+比例两列（表体两列不得并掉）。**弹性加宽主列**：左缘在主列、偏长伸进旁列空格且旁列仍由其它行支撑 → 不跨，扩主列范围。**旁列列头**：仅当压到邻近独立列头（合计/最大损失敞口）才从跨度剔除；表体科目行不得挡真交叉。父级通栏（账面余额/年头）除外。**并列同文核**不得盖成 `⟦↔⟧`。
9. **表外叙述不得入表**：`…如下：` / 通栏说明句不得进 `_source_words` 或并进金额行；**不得增删改表内已有单元格内容与顺序**。**连续 ≥3 行通栏且无金额** → 整块表外（1～2 行短通栏表头可保留）。折行合并不得把未闭合括号的长说明并进科目格。带冒号的结构小节（`核心一级资本：` / `…：扣除项`）在核证据下单独成行并跨列；**短词「资产」等不得词表预判为分组标题**——「资产管理计划」等一体核必须保持完整，折行半截按几何并回。**「其中：…」是表体层次科目**，必须与同行序号/金额同簇，不得因冒号当小节拆行。

辅助启发式（如「正常列≈表头+数据」「单点无表头可疑」）只能在**不违反以上铁律**时剪幽灵缝；不得把同界核拆开，不得为凑启发式牺牲分列结果。

改 `column_infer.py` / `assign_*` / `row_cluster` 时：先问是否仍满足总纲 + 铁律。不满足则改方案，禁止用后处理补丁掩盖。

## 次：跨格标注、表头回挂、空列清理

仅在**凝结核网格已定**之后：

| 模块 | 允许 | 禁止 |
|------|------|------|
| `span_mark.py` | 仅在凝结核交叉成立时标 `⟦↔N⟧` / `⟦↔⟧`；真通栏可挪左首格 | 无核证据强制跨列；增删列；把右列「数额/金额」当通栏挪到第一列 |
| `header_align.py` | 既有列内填表头、拆粘连文案；**期限分档短表头不得并格** | 并 orphan 列改列结构；把无期限/&lt;6/6-12/≥1年粘成一格 |
| pipeline 后处理 | 删真正全空列、剥表外页眉页脚 | 为「更好看」重跑分列 |

**口诀：先分列，再标注；标注服从分列，绝不反向改分列。**

## 改代码时

- 漏检跨格 → 只动 `span_mark`，不要为凑跨格去动并槽。
- 分列错 / 同界被拆 / 缩进多列 → 动 `column_infer`，以本文件总纲为准。
- 表格质检类改动仍走 `anomaly-rules-change` 技能（preflight / verify）。

---
description: 用户随机提问时，自动识别表格质检类任务并走防回归流程（用户无需记规范）
alwaysApply: true
---

# 表格质检 — 意图路由（零负担）

用户**不会**按固定格式提问。你负责识别意图并自动执行防护，**不要**要求用户填 scope、跑命令或读文档。

## 何时触发（满足任一即可）

用户话里出现或隐含：
- 表格质检 / 异常表 / 正常表 / 漏检 / 误报 / 🔴 / 红点
- 表头、折行、空白行/列、报告期、序号列、规则 C01～C04 / R01～R09
- `table_anomaly`、`needs_review`、`rule_ids`、黄金样本
- 「这页表为什么没标红」「修一下检测规则」等
- 凝结核 / 分列 / 跨格 / `⟦↔` / 缩进拆列 / 公共边界

涉及凝结核时另读并遵守 `.cursor/rules/grid-nucleus-priority.mdc`：
**分列为主，跨格标注为次，不得为标跨格破坏分列。**

## 触发后你必须做（用户无感）

1. 读取 `.cursor/skills/anomaly-rules-change/SKILL.md` 并执行
2. 用用户原话自动写 scope：`python -m codes.v2_steps.anomaly_change_guard init-scope "用户原话摘要"`
3. 改代码前跑 `preflight`；改后跑 `verify`
4. 交付时附防护结果；**不要**把流程步骤甩给用户

## 未触发时

与表格质检无关的问题（UI 样式、PDF 解析、部署等）→ 正常回答，不跑 guard。

## 用户只需

随口说目标或现象，例如：「第 5 页有表头还报 C01」。其余由你完成。

# DocuTable 分阶段改造方案（结构守恒 / 可控修复）

> 验收数据：`data/mid_cache/2025-04-29-601838_*成都银行*/liteparse/`
> - `pages.json`：Table Engine 输入（权威）
> - `pages/page_XXX.txt`：人工对照底片

## 阶段 A（本轮落地）— 小改动、高杠杆

目标：导出不断溯源；严重结构洞可测；用真实 P14/P28 验收。

| 项 | 内容 | 文件 |
|----|------|------|
| A1 | legacy 导出附带 `_cell_source_items[r][c]` | `export/legacy_adapter.py` |
| A2 | legacy ↔ Structured 校验含溯源非空率 | `legacy_adapter.py` |
| A3 | 粘连表头拆分已在 TE 内；补真实页回归脚本 | `_validate_cdbank_pages.py` |
| A4 | 重点洞：小节误并表、粘连表头、折行增减、变化原因列 | 既有 TE 修复 + 本轮验证 |

**不做**：大改 UI、真 merge 单元格、重写解析器。

## 阶段 B（下一轮）

- UI：对照 `page_XXX.txt` + 白名单结构操作（拆列/纵并）
- 保存前：用 `_cell_source_items` 跑守恒，失败禁存
- 规则修复优先走 StructuredTable，少改纯 `data[][]`

## 阶段 C（可选）

- 表头 `col_span` 仅显示层
- 行层次 metadata（小节/折行/数据）
- 排障 Agent（绑 pages.json + 测试）

## 严重问题优先级

1. **导出丢 `source_items`** → 人工/规则修表无法回滚（A1）
2. **粘连表头 / 折行表头** → 列错位（已修 + A3 验收）
3. **小节（二）（三）误并 region** → 整表搅乱（已修 + A3 验收）
